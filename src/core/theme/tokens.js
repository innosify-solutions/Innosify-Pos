/**
 * Design token definitions.
 * CSS custom properties in @assets/styles/tokens.css are the runtime source of truth.
 * Use this file for JS-accessible token values when needed.
 */

export const designTokens = {
  spacing: {
    shell: 'var(--spacing-shell)',
    sidebar: 'var(--spacing-sidebar)',
  },
  radius: {
    default: 'var(--radius-default)',
    lg: 'var(--radius-lg)',
  },
};
