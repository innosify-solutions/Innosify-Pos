/**
 * Tauri/native desktop capability boundary.
 * All Tauri invoke calls must go through this service layer.
 */

export const desktopService = {
  // Placeholder for future Tauri command wrappers
};
