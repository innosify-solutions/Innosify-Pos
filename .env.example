# VITE_BUSINESS_TYPE=retail

# URL of the Express backend. Leave empty to use same-origin /api
# (Vite proxies /api to http://localhost:3001 during `npm run dev`).
# VITE_API_URL=http://localhost:3001/api
